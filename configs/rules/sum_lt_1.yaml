type: SUM_LT_1
name: "Multi-Outcome < $1"
enabled: true
scope:
  platforms: ["polymarket"]
thresholds:
  sum_price_lt: 1.0
  min_liquidity: 300.0
dedupe:
  cooldown_secs: 600
outputs:
  level: "P1"
  score:
    base: 70
    weights:
      liquidity: 0.4
      time_to_end: 0.3
      spread: 0.3
